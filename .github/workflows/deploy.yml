name: Deploy on Local Machine

on:
     push:
          branches: [ "main" ]

jobs:
     deploy:
          runs-on: self-hosted

          steps:
             - name: Checkout code
               uses: actions/checkout@v3

             - name: Update and Restart Containers
               run: |
                    ls
